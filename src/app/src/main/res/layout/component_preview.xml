<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/grid_frame_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.gabrielbmoro.crazymath.presentation.components.grid.GridViewComponent
        android:id="@+id/grid_preview_component"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginBottom="@dimen/preview_margin_bottom"
        android:minHeight="@dimen/preview_min_height" />

    <TextView
        android:id="@+id/tv_preview_description"
        style="@style/PreviewDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center"
        android:visibility="gone"
        tools:text="Starting Dragging"
        tools:visibility="visible" />
</FrameLayout>